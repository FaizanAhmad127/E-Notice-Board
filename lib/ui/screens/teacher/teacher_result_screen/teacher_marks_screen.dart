import 'package:flutter/material.dart';

class TeacherMarksScreen extends StatelessWidget {
  const TeacherMarksScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('marks screen'),
    );
  }
}
